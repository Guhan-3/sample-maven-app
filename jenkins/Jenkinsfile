pipeline {
    agent {
  label 'Devserver'
}
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
            post {
            success {
                archiveArtifacts artifacts:'**/target/*.war'
                 }
            }
        }
    }
}
